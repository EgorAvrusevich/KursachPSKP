# Поднятие архитектуры
docker-compose down -v # Удалит данные(полностью положет контейнеры)
docker-compose up --build

# Если очистил бд, надо создать нувую бд в контейнере
docker exec -it recruiting_db /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Kv6084242kv! -C -Q "CREATE DATABASE [recruiting_db];"


1. Модели данных (Sequelize)
Промпт: «Напиши код для определения моделей Sequelize на основе следующей схемы БД: Users, Profiles, Vacancies, CheckListTemplates, Applications, CandidateProgress, ChatMessages. Используй диалект MSSQL. Для каждой модели укажи типы данных, ограничения (allowNull, unique) и, самое главное, пропиши ассоциации (hasMany, belongsTo, hasOne) в отдельном файле или методе associate. Учти, что UserId в Profiles связан 1:1 с Users.»

2. Аутентификация и Роли
Промпт: «Реализуй систему аутентификации для моего проекта. Мне нужны:

Контроллер регистрации (с хешированием пароля через bcryptjs).

Контроллер логина, который выдает JWT токен.

Middleware authenticateToken, который проверяет JWT.

Middleware authorizeRole(['Admin', 'Recruiter']), который ограничивает доступ к маршрутам в зависимости от роли пользователя, хранящейся в токене.
Используй архитектуру Express.js.»

3. API для Вакансий и Откликов
Промпт: «Создай REST API эндпоинты для управления вакансиями и откликами.
Для рекрутера: POST /vacancies (создание с автоматическим созданием шаблонов этапов из массива), GET /vacancies (свои вакансии).
Для кандидата: GET /vacancies (все активные), POST /vacancies/:id/apply (создание записи в Applications и инициализация всех этапов в CandidateProgress со статусом false).
Используй Sequelize транзакции, чтобы запись в несколько таблиц была атомарной.»

4. Интеграция с RabbitMQ (Уведомления)
Промпт: «Настрой работу с RabbitMQ в моем приложении. Мне нужен модуль producer.js, который отправляет сообщение в очередь 'notifications' при создании нового отклика (Application). Также напиши отдельный файл воркера emailWorker.js, который слушает эту очередь и выводит в консоль (имитируя отправку email) сообщение: 'Новый отклик на вакансию [Title] от пользователя [Email]'. Учти, что подключение к RabbitMQ должно быть устойчивым к сбоям (используй логику переподключения).»

5. Реал-тайм Чат (Socket.io)
Промпт: «Добавь функционал чата на Socket.io в мой проект. При подключении клиент должен передавать свой JWT для идентификации. Сообщения должны сохраняться в таблицу ChatMessages через Sequelize. Реализуй событие 'private_message', которое отправляет сообщение конкретному пользователю по его ID. Если пользователь оффлайн — просто сохраняй в БД. Напиши серверную часть на Node.js и пример простого клиентского подключения.»

6. Документация (Swagger)
Промпт: «Настрой Swagger (swagger-jsdoc и swagger-ui-express) для моего API. Мне нужно, чтобы документация была доступна по адресу /api-docs. Опиши схему для авторизации через Bearer Token (JWT) и добавь примеры документации (JSDoc) для эндпоинтов логина и получения списка вакансий.»

"Пиши код с учетом того, что база данных находится в Docker-контейнере с хостом 'db' и портом 1433, а RabbitMQ — на хосте 'mq'."




1. Глобальная структура и UI-кит
Этот промпт создаст фундамент приложения, чтобы все последующие компоненты выглядели единообразно.

Промпт: "Создай базовую структуру React-приложения для платформы по поиску работы 'HireVich'. Используй Vite, Tailwind CSS и библиотеку иконок Lucide-react. Настрой общую цветовую палитру (основной цвет — глубокий синий #1e40af, акцентный — бирюзовый). Реализуй систему Layout: Navbar (логотип, навигация, профиль) и Footer. Добавь кастомные UI-компоненты: Button (с вариантами primary, secondary, outline), Input с поддержкой ошибок, Card и Modal. Все компоненты должны быть адаптивными."

2. Авторизация и Роли (JWT Integration)
Здесь важно связать интерфейс с твоей логикой ролей (Admin, Recruiter, Candidate).

Промпт: "Реализуй страницы Входа и Регистрации на React. В регистрации должно быть переключение ролей: Кандидат (ищет работу) или Рекрутер (размещает вакансии). Используй react-hook-form и Zod для валидации (email, пароль мин. 8 символов, ФИО). После логина токен должен сохраняться в localStorage. Реализуй защищенный роут (ProtectedRoute), который проверяет наличие токена и роль пользователя, прежде чем пустить на страницу Dashboard."

3. Личный кабинет Кандидата (Поиск и Отклики)
Основной функционал для обычного пользователя.

Промпт: "Создай главную страницу поиска вакансий. Слева — панель фильтров (категория, тип занятости: удаленка/офис, диапазон зарплаты). Справа — список карточек вакансий. При клике на карточку открывается детальная страница с описанием, требованиями и большой кнопкой 'Откликнуться'. Также добавь страницу 'Мои отклики', где в виде таблицы или списка отображается статус (Ожидание, Просмотр, Отказ, Приглашение)."

4. Панель Рекрутера (Управление вакансиями и кандидатами)
Самая сложная часть с точки зрения логики данных.

Промпт: "Разработай Dashboard для Рекрутера.

Секция управления: Список созданных вакансий с возможностью редактирования, архивации и создания новой через форму.

Просмотр откликов: Интерфейс в стиле Kanban-доски или детального списка, где видны кандидаты, прикрепившие свои резюме к конкретной вакансии.

Добавь статистику: сколько активных вакансий и сколько новых откликов за неделю."

5. Административная панель
Для управления всей системой.

Промпт: "Создай Admin Panel для управления пользователями и модерации вакансий. Таблица всех пользователей с поиском по email и возможностью заблокировать или изменить роль. Добавь лог последних действий в системе (кто зарегистрировался, какая вакансия создана). Дизайн должен быть строгим и информативным."

6. Уведомления в реальном времени (Интеграция с RabbitMQ)
Поскольку у тебя на бэкенде есть RabbitMQ, фронтенд должен уметь показывать алерты (например, через Socket.io).

Промпт: "Создай компонент 'Notification Center' (выпадающий список по клику на колокольчик в Navbar). Реализуй визуальную часть для разных типов уведомлений: 'Новый отклик', 'Статус вакансии изменен', 'Системное сообщение'. Напиши кастомный хук useNotifications, который будет имитировать получение данных по веб-сокетам и обновлять счетчик непрочитанных сообщений."

Полезные советы по сборке:
Запросы (API): Попроси AI написать конфиг для axios с интерцептором, который автоматически подставляет Authorization: Bearer <token> во все запросы.

Стейт-менеджмент: Для такого проекта отлично подойдет Zustand — он проще Redux и идеально подходит для хранения данных о текущем пользователе.

Docker-связка: При генерации кода делай акцент на том, что API доступен по адресу http://localhost:3000/api (или просто /api, если настроишь прокси в Vite/Nginx).